sudo: required
language: node_js
node_js:
- '8'
cache:
  directories:
  - "~/.npm"
  - "~/.cache"
services:
- docker
script:
- docker-compose build
deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master
notifications:
  email: true
